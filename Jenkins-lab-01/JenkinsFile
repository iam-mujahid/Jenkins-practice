pipeline {

    agent {
        docker {
            image 'node:20-alpine'
            args '-u 0:0'
        }
    }

    stages {

        stage('Docker Agent') {
            steps {

                sh 'echo "=== SYSTEM ==="'
                sh 'whoami'
                sh 'id'
                sh 'hostname'
                sh 'pwd'

                sh 'echo "=== NODE ==="'
                sh 'node --version'
                sh 'npm --version'

                sh 'echo "=== OS ==="'
                sh 'cat /etc/os-release'

                sh 'echo "=== WORKSPACE ==="'
                sh 'ls -la'
            }
        }
    }
}
